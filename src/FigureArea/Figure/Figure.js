"use strict";
exports.__esModule = true;
exports.Figure = void 0;
var Figure = /** @class */ (function () {
    function Figure(name, color) {
        this.name = name;
        this.color = color;
        // protected constructor(public readonly name: string, public readonly color: string, protected a: number, protected b: number);
        // protected constructor(public readonly name: string, public readonly color: string, protected a: number, protected b: number, protected c: number);
        // protected constructor(public readonly name: string, public readonly color: string, protected a: number, protected b?: number, protected c?: number) {
        // if (b) {
        //   this.b = b;
        // }
        // if (c) {
        //   this.c = c;
        // }
    }
    return Figure;
}());
exports.Figure = Figure;
